# No `partition_table` here: it differs per build profile, so
# scripts/flash.sh passes the right one via `--partition-table`.

[connection]

[[usb_device]]
vid = "1a86"
pid = "7522"

[[usb_device]]
vid = "0403"
pid = "6001"

[flash]